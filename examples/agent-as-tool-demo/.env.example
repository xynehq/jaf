# Agents-as-Tools Demo Environment Variables
# Copy this file to .env and fill in values as needed.

# LiteLLM proxy configuration
# Use your LiteLLM server URL. Include /v1 if required by your setup.
LITELLM_URL=http://localhost:4000/v1
LITELLM_API_KEY=sk-your-key

# Default chat model to use via LiteLLM
LITELLM_MODEL=gpt-4o-mini

# Server settings
PORT=3000

# Memory provider (defaults to in-memory if unset)
# Options: memory | redis | postgres
JAF_MEMORY_TYPE=memory

# If using Redis memory provider, uncomment and configure:
# JAF_REDIS_HOST=localhost
# JAF_REDIS_PORT=6379
# JAF_REDIS_PASSWORD=
# JAF_REDIS_DB=0
# JAF_REDIS_PREFIX=jaf:memory:
# JAF_REDIS_TTL=

# If using PostgreSQL memory provider, uncomment and configure:
# JAF_POSTGRES_HOST=localhost
# JAF_POSTGRES_PORT=5432
# JAF_POSTGRES_DB=jaf_memory
# JAF_POSTGRES_USER=postgres
# JAF_POSTGRES_PASSWORD=
# JAF_POSTGRES_SSL=false
# JAF_POSTGRES_TABLE=conversations
# JAF_POSTGRES_MAX_CONNECTIONS=10

